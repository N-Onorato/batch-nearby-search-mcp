# Google Maps API Key
# Get your key at: https://console.cloud.google.com/apis/credentials
# Required APIs: Distance Matrix API, Places API (New), Geocoding API
GOOGLE_MAPS_API_KEY=your-api-key-here

# Optional: Adjust caching TTL (in seconds)
# PLACES_CACHE_TTL=3600

# Optional: Rate limiting (max concurrent API calls)
# MAX_CONCURRENT_REQUESTS=10
